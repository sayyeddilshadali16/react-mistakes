// Server and Client component

//In Next JS we need use "use client" for client side and "use server" for server side if we don't use "use client" on the client side it will not compile or show our component on the client side and also would not keep up update of the states.

import React from 'react'

const Number3 = () => {
  return (
    <div>ServerComponentExample</div>
  )
}

export default Number3